{
  "name": "@vibe-box/computerd",
  "version": "0.1.0-alpha.1",
  "description": "Computer daemon CLI and FUSE mount. Source-only; the distributable lives at @vibe-box/computer-computerd-linux-x64.",
  "license": "MIT",
  "private": true,
  "type": "commonjs",
  "bin": {
    "computerd": "./dist/cli/computerd.cjs"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "build:bin": "node ./scripts/build-bin.mjs",
    "build:docker": "node ./scripts/build-docker.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "publish:docker": "node ./scripts/build-docker.mjs --push"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@platformatic/vfs": "^0.4.0",
    "@vibe-box/computer-rpc": "*",
    "@vibe-box/dofs": "*",
    "fuse-native": "^2.2.6"
  },
  "devDependencies": {
    "vitest": "^4.1.7"
  }
}
